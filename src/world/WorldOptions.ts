import * as MH from '../utils/MathHelper';
export default class WorldOptions {
    public static readonly mTileSize = MH.xToWorld(64);
    public static readonly pTileSize = 64;
    public static readonly ChunkSize = 64;
}